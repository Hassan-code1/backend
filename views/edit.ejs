<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/styles.css">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body>
    <div class="w-full h-full bg-zinc-900 text-white px-3 py-5">
        <div class="nav mb-10">
            <a class="text-blue-500" href="/read">Read users</a>
            <a class="text-blue-500 ml-3" href="/index">Back to home</a>
        </div>
        <h1 class="text-3xl tracking-tight mb-3">Update Users</h1>
        <form action="/update/<%= user._id %>" method="post">
            <input name="name" class="px-4 py-2 border-2 bg-transparent outline-none border-zinc-500 rounded-lg" type="text" placeholder="Enter your name" value="<%= user.name %>">
            <input name="email" class="px-4 py-2 border-2 bg-transparent outline-none border-zinc-500 rounded-lg" type="text" placeholder="Enter your email" value="<%= user.email %>">
            <input name="image" class="px-4 py-2 border-2 bg-transparent outline-none border-zinc-500 rounded-lg" type="text" placeholder="Enter your profile image url" value="<%= user.image %>">
            <button class="px-5 py-2 bg-yellow-500 text-black rounded-lg ml-3" type="submit">Update User</button>
        </form>
    </div>
</body>
</html>