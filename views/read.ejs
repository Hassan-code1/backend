<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/styles.css">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body>
    <div class="w-full h-full bg-black text-white px-3 py-5">
        <a class="text-blue-500" href="/">Back to Home</a>
        <h1 class="text-3xl mt-10 tracking-tighter mb-3">All Users</h1>
        <div class="users flex gap-3 flex-wrap">
            <% if(users.length == 0){ %>
               <h3>No Users Yet </h3>
            <% } %>
            <% users.forEach(function(user){ %>
                <div class="user w-72 p-4 bg-black-00 border-2 border-black-300 rounded-lg">
                    <div class="w-full h-52 bg-red-500 rounded-lg overflow-hidden" aria-label="User avatar placeholder">
                        <img class="w-full h-full object-cover" src="<%=user.image %>" alt="<%= user.name %>">
                    </div>
                    <h3 class="text-xl tracking-tighter mt-3"><%= user.name %></h3>
                    <h5 class="text-zinc-500"><%= user.email %></h5>
                    <div class="flex gap-3 mt-10">
                        <a class="text-zinc-200" href="/edit/<%= user._id %>">Edit User</a>
                        <a class="text-red-500" href="/delete/<%= user._id %>">Delete User</a>
                    </div>
                </div>
           <%  }) %>
        </div>
    </div>
</body>
</html>